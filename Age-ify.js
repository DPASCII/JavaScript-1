const yearOfBirth = 1994;

const yearFuture = 2050;
const age = yearFuture - yearOfBirth;

console.log("You will be " + age + " years old in " + yearFuture);

// alternatively
const str = `You will be ${age} years old in ${yearFuture}`;
